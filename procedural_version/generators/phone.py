# Импортируем функцию создания генератора случайности.
from procedural_version.utils.random_utils import create_random

# Объявляем функцию, которая генерирует номер телефона как словарь.
def generate_phone(valid=True, seed=None):
    # Что такое seed: это значение, которое делает случайный телефон повторяемым для тестов.
    # Если вызвать функцию два раза с одинаковым seed, случайный результат должен быть одинаковым.
    # Входные данные: valid - если True, телефон должен быть правильным; если False, телефон должен быть с ошибкой.
    # Входные данные: seed - значение для повторяемой генерации.
    # Внутренние переменные: randomizer - генератор случайности, который создается через create_random(seed).
    # Внутренние переменные: country_code - код страны телефона.
    # Внутренние переменные: operator_code - код оператора телефона.
    # Внутренние переменные: number - основная часть номера телефона.
    # Выходные данные: функция должна вернуть словарь с ключами country_code, operator_code и number.
    # Шаг 1. Создай randomizer через create_random(seed), чтобы номер телефона можно было повторить.
    # Шаг 2. Если valid равен False, собери словарь с намеренно неправильным телефоном.
    # Шаг 3. Для неправильного телефона используй неверный country_code и слишком короткий number.
    # Шаг 4. Если valid равен True, собери словарь с правильным country_code "+7".
    # Шаг 5. Для правильного телефона сгенерируй operator_code из трех цифр.
    # Шаг 6. Для правильного телефона сгенерируй number из семи цифр.
    # Шаг 7. Верни готовый словарь телефона.
    # Проверка после реализации: запусти python -m unittest procedural_version.tests.test_generators.ProceduralGeneratorsTest.test_generate_phone_can_return_valid_and_invalid_values
    # Если все хорошо, в конце вывода unittest будет написано OK.
    # Если тест упал, проверь country_code и длину основной части номера для valid=True и valid=False.
    pass
