# Импортируем список слов для генерации имени пользователя.
from procedural_version.data.names_data import USERNAME_WORDS
# Импортируем функцию создания генератора случайности.
from procedural_version.utils.random_utils import create_random

# Объявляем функцию, которая генерирует username пользователя.
def generate_username(length=10, seed=None):
    # Что такое seed: это подсказка для random, чтобы случайный результат можно было повторить.
    # Например, если два раза вызвать функцию с seed=1, результат должен получиться одинаковым.
    # Входные данные - это значения в скобках функции.
    # Входные данные: length - точное количество символов в username.
    # Входные данные: seed - значение для повторяемой генерации.
    # Переменные внутри функции можно называть по-своему.
    # Тесты проверяют результат функции, а не названия переменных.
    # Ниже перечислены примеры понятных названий переменных.
    # Внутренние переменные: randomizer - генератор случайности, который создается через create_random(seed).
    # Внутренние переменные: username_word - учебное слово для начала username.
    # Внутренние переменные: alphabet - строка разрешенных символов для username.
    # Внутренние переменные: username - готовая строка username.
    # Выходные данные - это значение, которое функция отдает с помощью return.
    # Выходные данные: функция должна вернуть строку длиной ровно length символов.
    # Шаг 1. Проверь, что length больше 0, потому что username должен содержать хотя бы один символ.
    # Шаг 2. Создай randomizer через create_random(seed), чтобы username можно было повторить.
    # Шаг 3. Выбери учебное слово из USERNAME_WORDS для начала username.
    # Шаг 4. Подготовь разрешенные символы: маленькие буквы, цифры и нижнее подчеркивание.
    # Шаг 5. Добавляй случайные символы, пока username не станет нужной длины.
    # Шаг 6. Обрежь username ровно до length символов.
    # Шаг 7. Верни готовый username.
    # Как проверить работу: запусти в терминале python -m unittest procedural_version.tests.test_generators.ProceduralGeneratorsTest.test_generate_username_returns_exact_length
    # Если все правильно, в самом конце появится слово OK.
    # Если вместо OK появилась ошибка, проверь, что длина username ровно равна length.
    pass
