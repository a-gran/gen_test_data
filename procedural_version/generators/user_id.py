# Импортируем функцию создания генератора случайности.
from procedural_version.utils.random_utils import create_random

# Объявляем функцию, которая генерирует цифровой ID пользователя.
def generate_user_id(length=6, only_digits=True, seed=None):
    # Где почитать про эту функцию: открой docs/function_specifications.md и найди раздел generate_user_id.
    # Где посмотреть задание команды: открой docs/team_tasks.md и найди generate_user_id.
    # Где посмотреть пример использования: открой docs/usage.md и найди generate_user_id.
    # seed помогает получать одинаковый случайный результат.
    # Например, generate_user_id(seed=1) и еще раз generate_user_id(seed=1) должны вернуть один и тот же ID.
    # Это удобно для тестов: тест знает, какой результат должен получиться.
    # Если seed=None, результат может быть разным при каждом запуске.
    # Входные данные - это значения в скобках функции.
    # Входные данные: length - сколько символов должно быть в ID.
    # Входные данные: only_digits - если True, ID состоит только из цифр; если False, можно добавить буквы.
    # Входные данные: seed - число для повторения случайного результата или None.
    # Переменные внутри функции можно называть по-своему.
    # Тесты проверяют результат функции, а не названия переменных.
    # Ниже перечислены примеры понятных названий переменных.
    # Внутренние переменные: randomizer - генератор случайности, который создается через create_random(seed).
    # Внутренние переменные: alphabet - строка символов, из которых можно собирать ID.
    # Внутренние переменные: user_id - готовая строка ID.
    # Выходные данные - это значение, которое функция отдает с помощью return.
    # Выходные данные: функция должна вернуть строку длиной ровно length символов.
    # Шаг 1. Проверь, что length больше 0, потому что ID должен содержать хотя бы один символ.
    # Шаг 2. Создай randomizer через create_random(seed), чтобы ID можно было повторить.
    # Шаг 3. Подготовь строку с цифрами, которые можно использовать в ID.
    # Шаг 4. Если only_digits равен False, добавь к цифрам английские буквы.
    # Шаг 5. Собери строку из length случайных символов.
    # Шаг 6. Верни готовый ID.
    # Как проверить работу: запусти в терминале python -m unittest procedural_version.tests.test_generators.ProceduralGeneratorsTest.test_generate_user_id_returns_digits_with_exact_length
    # Дополнительная проверка: запусти python -m unittest procedural_version.tests.test_generators.ProceduralGeneratorsTest.test_generate_user_id_can_return_letters_and_digits
    # Если все правильно, в самом конце появится слово OK.
    # Если вместо OK появилась ошибка, проверь длину ID и набор разрешенных символов.
    pass
