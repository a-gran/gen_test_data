# Импортируем список фамилий для генерации случайной фамилии.
from procedural_version.data.names_data import LAST_NAMES
# Импортируем функцию создания генератора случайности.
from procedural_version.utils.random_utils import create_random

# Объявляем функцию, которая генерирует случайную фамилию.
def generate_last_name(min_length=None, max_length=None, seed=None):
    # Что такое seed: это значение, которое делает случайный выбор фамилии повторяемым для тестов.
    # Если вызвать функцию два раза с одинаковым seed, случайный результат должен быть одинаковым.
    # Входные данные: min_length - минимальная длина фамилии или None без нижнего ограничения.
    # Входные данные: max_length - максимальная длина фамилии или None без верхнего ограничения.
    # Входные данные: seed - значение для повторяемой генерации.
    # Внутренние переменные: randomizer - генератор случайности, который создается через create_random(seed).
    # Внутренние переменные: filtered_last_names - список фамилий, которые подходят под ограничения длины.
    # Выходные данные: функция должна вернуть строку с фамилией из LAST_NAMES.
    # Шаг 1. Создай randomizer через create_random(seed), чтобы выбор фамилии можно было повторить.
    # Шаг 2. Возьми полный список фамилий LAST_NAMES и сохрани его в отдельную переменную.
    # Шаг 3. Если min_length передан, оставь только фамилии не короче этого значения.
    # Шаг 4. Если max_length передан, оставь только фамилии не длиннее этого значения.
    # Шаг 5. Если после фильтрации список пустой, вызови понятную ошибку ValueError.
    # Шаг 6. Верни случайную фамилию из подходящего списка.
    # Проверка после реализации: запусти python -m unittest procedural_version.tests.test_generators.ProceduralGeneratorsTest.test_generate_last_name_respects_length_filter
    # Если все хорошо, в конце вывода unittest будет написано OK.
    # Если тест упал, проверь, что фамилия взята из LAST_NAMES и подходит под ограничения длины.
    pass
