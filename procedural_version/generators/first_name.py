# Импортируем список имен для генерации случайного имени.
from procedural_version.data.names_data import FIRST_NAMES
# Импортируем функцию создания генератора случайности.
from procedural_version.utils.random_utils import create_random

# Объявляем функцию, которая генерирует случайное имя.
def generate_first_name(min_length=None, max_length=None, seed=None):
    # Что такое seed: это подсказка для random, чтобы случайный результат можно было повторить.
    # Например, если два раза вызвать функцию с seed=1, результат должен получиться одинаковым.
    # Входные данные - это значения в скобках функции.
    # Входные данные: min_length - минимальная длина имени или None без нижнего ограничения.
    # Входные данные: max_length - максимальная длина имени или None без верхнего ограничения.
    # Входные данные: seed - значение для повторяемой генерации.
    # Переменные внутри функции можно называть по-своему.
    # Тесты проверяют результат функции, а не названия переменных.
    # Ниже перечислены примеры понятных названий переменных.
    # Внутренние переменные: randomizer - генератор случайности, который создается через create_random(seed).
    # Внутренние переменные: filtered_names - список имен, которые подходят под ограничения длины.
    # Выходные данные - это значение, которое функция отдает с помощью return.
    # Выходные данные: функция должна вернуть строку с именем из FIRST_NAMES.
    # Шаг 1. Создай randomizer через create_random(seed), чтобы выбор имени можно было повторить.
    # Шаг 2. Возьми полный список имен FIRST_NAMES и сохрани его в отдельную переменную.
    # Шаг 3. Если min_length передан, оставь только имена не короче этого значения.
    # Шаг 4. Если max_length передан, оставь только имена не длиннее этого значения.
    # Шаг 5. Если после фильтрации список пустой, вызови понятную ошибку ValueError.
    # Шаг 6. Верни случайное имя из подходящего списка.
    # Как проверить работу: запусти в терминале python -m unittest procedural_version.tests.test_generators.ProceduralGeneratorsTest.test_generate_first_name_respects_length_filter
    # Если все правильно, в самом конце появится слово OK.
    # Если вместо OK появилась ошибка, проверь, что имя есть в FIRST_NAMES и подходит по длине.
    pass
