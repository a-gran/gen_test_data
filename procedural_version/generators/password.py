# Импортируем список слов для генерации учебного пароля.
from procedural_version.data.names_data import PASSWORD_WORDS
# Импортируем функцию создания генератора случайности.
from procedural_version.utils.random_utils import create_random

# Объявляем функцию, которая генерирует учебный пароль.
def generate_password(length=12, use_digits=True, use_symbols=True, seed=None):
    # seed помогает получать одинаковый случайный результат.
    # Например, generate_password(seed=1) и еще раз generate_password(seed=1) должны вернуть один и тот же пароль.
    # Это удобно для тестов: тест знает, какой результат должен получиться.
    # Если seed=None, результат может быть разным при каждом запуске.
    # Входные данные - это значения в скобках функции.
    # Входные данные: length - точная длина итогового пароля.
    # Входные данные: use_digits - если True, в пароле должна быть хотя бы одна цифра.
    # Входные данные: use_symbols - если True, в пароле должен быть хотя бы один специальный символ.
    # Входные данные: seed - число для повторения случайного результата или None.
    # Переменные внутри функции можно называть по-своему.
    # Тесты проверяют результат функции, а не названия переменных.
    # Ниже перечислены примеры понятных названий переменных.
    # Внутренние переменные: randomizer - генератор случайности, который создается через create_random(seed).
    # Внутренние переменные: letters - строка букв для пароля.
    # Внутренние переменные: digits - строка цифр для пароля.
    # Внутренние переменные: symbols - строка специальных символов для пароля.
    # Внутренние переменные: password_parts - обязательные части пароля.
    # Внутренние переменные: alphabet - все символы, из которых можно добирать пароль.
    # Внутренние переменные: raw_password - пароль до финального обрезания до length.
    # Выходные данные - это значение, которое функция отдает с помощью return.
    # Выходные данные: функция должна вернуть строку с паролем длиной ровно length символов.
    # Шаг 1. Проверь, что length больше 0, потому что пароль должен содержать хотя бы один символ.
    # Шаг 2. Создай randomizer через create_random(seed), чтобы пароль можно было повторить.
    # Шаг 3. Подготовь строки с буквами, цифрами и специальными символами.
    # Шаг 4. Начни пароль с учебного слова из PASSWORD_WORDS.
    # Шаг 5. Если use_digits равен True, добавь в пароль хотя бы одну цифру.
    # Шаг 6. Если use_symbols равен True, добавь в пароль хотя бы один специальный символ.
    # Шаг 7. Добери пароль случайными разрешенными символами до нужной длины.
    # Шаг 8. Верни пароль ровно length символов.
    # Как проверить работу: запусти в терминале python -m unittest procedural_version.tests.test_generators.ProceduralGeneratorsTest.test_generate_password_respects_required_parts
    # Если все правильно, в самом конце появится слово OK.
    # Если вместо OK появилась ошибка, проверь длину пароля, цифру и специальный символ.
    pass
