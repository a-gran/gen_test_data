# Импортируем список комментариев для генерации случайного комментария.
from procedural_version.data.names_data import COMMENTS
# Импортируем функцию создания генератора случайности.
from procedural_version.utils.random_utils import create_random

# Объявляем функцию, которая генерирует комментарий любой заданной длины.
def generate_comment(length=100, seed=None):
    # Где почитать про эту функцию: открой docs/function_specifications.md и найди раздел generate_comment.
    # Где посмотреть задание команды: открой docs/team_tasks.md и найди generate_comment.
    # Где посмотреть пример использования: открой docs/usage.md и найди generate_comment.
    # seed помогает получать одинаковый случайный результат.
    # Например, generate_comment(seed=1) и еще раз generate_comment(seed=1) должны вернуть один и тот же текст.
    # Это удобно для тестов: тест знает, какой результат должен получиться.
    # Если seed=None, результат может быть разным при каждом запуске.
    # Входные данные - это значения в скобках функции.
    # Входные данные: length - точное количество символов в итоговом комментарии.
    # Входные данные: seed - число для повторения случайного результата или None.
    # Переменные внутри функции можно называть по-своему.
    # Тесты проверяют результат функции, а не названия переменных.
    # Ниже перечислены примеры понятных названий переменных.
    # Внутренние переменные: randomizer - генератор случайности, который создается через create_random(seed).
    # Внутренние переменные: base_comment - случайный текст из COMMENTS, который берется за основу.
    # Внутренние переменные: repeated_comment - повторенный текст, из которого потом берется нужная длина.
    # Выходные данные - это значение, которое функция отдает с помощью return.
    # Выходные данные: функция должна вернуть строку длиной ровно length символов.
    # Шаг 1. Проверь, что length не меньше 0, потому что длина строки не может быть отрицательной.
    # Шаг 2. Создай randomizer через create_random(seed), чтобы комментарий можно было повторить.
    # Шаг 3. Выбери базовый текст из списка COMMENTS.
    # Шаг 4. Повтори базовый текст несколько раз, чтобы символов точно хватило.
    # Шаг 5. Обрежь получившуюся строку ровно до length символов.
    # Шаг 6. Верни готовый комментарий.
    # Как проверить работу: запусти в терминале python -m unittest procedural_version.tests.test_generators.ProceduralGeneratorsTest.test_generate_comment_returns_exact_length
    # Дополнительная проверка: запусти python -m unittest procedural_version.tests.test_generators.ProceduralGeneratorsTest.test_generate_comment_returns_any_requested_length
    # Если все правильно, в самом конце появится слово OK.
    # Если вместо OK появилась ошибка, проверь, что длина строки всегда ровно равна length.
    pass
